{
  "name": "agentic-collaboration",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "start": "npm run start --prefix server",
    "deploy:server": "npm run typecheck --prefix server && railway up --detach",
    "deploy:client": "cd client && VITE_BACKEND_URL=https://backend-production-370a.up.railway.app npm run build && wrangler pages deploy dist --project-name=agentic-collaboration-demo --branch=main --commit-dirty=true",
    "deploy": "npm run deploy:server && npm run deploy:client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
